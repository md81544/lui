* "live" letter count when entering search string

* Colon entry in input box should cancel entry and initiate command mode

* Maybe a config file for colours? Currently doesn't display very
  nicely on a terminal configured with a light background
  - or a "light mode" (or monochrome) switcher?

* Implement clue number entry

* Maybe move the command entry to the very last row

* Think about adding a help screen

* Both of these should have an immediate input rather than
  kludging an entry out of search string:
  * Implement thesaurus lookup
  * Implement "define" (dictionary definition lookup)

* Implement load & save (maybe a picker for load?)

* Considering the kludge required to hide tmux's immediate printing
  of pasted text, it might be an idea to rethink output. Instead of
  a large string with all the text, we could have a "UiElement" class
  which contains row, col, text (inc ANSI codes). A render would just
  work through a vector of these to generate output. If we need to
  clear a line when in immediate mode (i.e. input()) we could clear
  it and then re-render any elements which share that line.