* Add unit tests for WordSearcher

* Implement scrolling of the results pane by mouse

* Make "_Regular" "Re_gular" and make "^Restart" "_Restart".
  - Restart should check dirty flag (see below)

* Remove clue from saved? Maybe some form of "done"?

* Empty entry of clue number in load() should just quit, not show "Cannot find clue ''"

* Need a "remove" option. Maybe results pane displays remaining letters?

* Allow number entry in unconstrained "found" input. E.g. entering 4 would insert "...."

* Add "dirty" flag if clue isn't saved but has changed (might need interactive
  messageBox - see below)

* Undo filter?

* Multi-line message box. Include option to wait for, and return, keypress (use input)

* Maybe improve definitions display? Currently just dumps a vector.
* (related) allow horizontal scrolling of results (or at least the definitions portion)

* Add a generic pre-hook function (to be called from pre-hooks) which can
  initially just check for ':' perhaps? Wouldn't be called from free-format entry
  like the "comment" entry.

* Immediate mode messageBox with a "press Enter" to dismiss

* Colon entry in input box should cancel entry and initiate command mode
  - maybe not... what about free-form input?
  - although could work as an option flag, usable in non-comment input

* Maybe a config file for colours? Currently doesn't display very
  nicely on a terminal configured with a light background
  - or a "light mode" (or monochrome) switcher?

* Think about adding a help screen

* Considering the kludge required to hide tmux's immediate printing
  of pasted text, it might be an idea to rethink output. Instead of
  a large string with all the text, we could have a "UiElement" class
  which contains row, col, text (inc ANSI codes). A render would just
  work through a vector of these to generate output. If we need to
  clear a line when in immediate mode (i.e. input()) we could clear
  it and then re-render any elements which share that line.