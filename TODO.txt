* Log should restart each run, perhaps with a backup of the previous?

* Consider how best to deal with a terminal that has a light colour scheme
  - maybe just use -c0 in that case

* Now it's easy to click or tab off a field, search string input should be
  "focused" by default on start/restart

* Cheat warning on, say, lookup (messageBox with "are you sure")

* Auto-save on clue number entry

* Shift-letter (or Ctrl-) in input should quit input and activate command

* Should be able to load a clue from the "load" list by clicking on item

* When removing letters from "remaining", favour position, e.g.
  if search string is "UNDUE" and found string is "..DU." then the 
  "remaining" string should be "UNE" not "NUE"

* When in input mode, dim the menu?

* If all letters in constrained found input have been entered, an automatic 
  enter should be generated

* keypress.h is now more than just a basic keypress handler
  - rename to, say, terminal_input or terminal_event
  - should now be a proper class
  - consider that this class should maybe run in a separate thread and handle
    all events received by the terminal and expose them as separate collections
    of different typed events, e.g. keypress, mouse, resize etc. (e.g. client
    code does a "readEvents" and responds to the ones it wants to, the rest
    are dropped on the next iteration)

* "More" menu? With numbered commands? Could just display a message box
  with numbered items, e.g. "1: Debug log/2: Add phrase" etc
  - "Add phrase" would need to check whether the current directory is
    git-controlled
    
* Clickable options on messageBox... buttons?

* Add regex check to ensure clue number is in the correct format
  - regex should be "^\d{1,3}[AD]$" (and enforce capitals in input)

* Scratchpad command - just allows free entry?

* (maybe) Multiple-character extended commands, e.g. :r! to restart without
  "are you sure?" message if m_clue is dirty

* Add reverse video in Terminal: on = "\e[7m", off = "\e[27m"

* multiple colour support in results pane?
  - may be just as simple as including colour codes?

* Add unit tests for WordSearcher

* Make "_Regular" "Re_gular" and make "^Restart" "_Restart".

* Undo filter? Although this can be done by just re-querying

* Maybe improve definitions display? Currently just dumps a vector.
* (related) allow horizontal scrolling of results (or at least the definitions portion)
* (related) consider returning something more structured from WordSearcher::define()
  to allow for the caller to add, say, colour to the output (e.g. definitions, words,
  and undefined to have separate colours)

* Add a generic pre-hook function (to be called from pre-hooks) which can
  initially just check for ':' perhaps? Wouldn't be called from free-format entry
  like the "comment" entry.

* Colon entry in input box should cancel entry and initiate command mode
  - maybe not... what about free-form input?
  - although could work as an option flag, usable in non-comment input

* Think about adding a help screen (F1 or :h)
