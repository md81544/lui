* Add light colour "theme" as config option

* When "jumble" is displayed, pressing space should re-jumble

* When clicking on a header input field to focus it, the cursor
  should be placed where the user clicked

* Optional time-out for messageBox

* "Grey out" Load menu item unless there are saved clues

* Dirty visual indicator?

* Terminal mode guard

* Improve definitions display
  - (related) consider returning something more structured from WordSearcher::define()
    to allow for the caller to add, say, colour to the output (e.g. definitions, words,
    and undefined to have separate colours)
  - (related) multiple colour support in results pane?
    - may be just as simple as including colour codes?

* keypress.h is now more than just a basic keypress handler
  - rename to, say, terminal_input or terminal_event
  - should now be a proper class
  - consider that this class should maybe run in a separate thread and handle
    all events received by the terminal and expose them as separate collections
    of different typed events, e.g. keypress, mouse, resize etc. (e.g. client
    code does a "readEvents" and responds to the ones it wants to, the rest
    are dropped on the next iteration)
    
* Add unit tests for everything

* Think about adding a help screen (F1 or :h)
